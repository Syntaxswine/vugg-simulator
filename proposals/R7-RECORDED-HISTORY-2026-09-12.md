# R7 remaining history sections

Continuation from `cf3ed5c7` (R7a), on canonical SIM 285. Scientific records
remain authoritative. An attractive population is not evidence for an event.

Subsequent increment: [dated coatings and buried surfaces](SURFACE-HISTORY-2026-09-12.md)
extends the original current-only film boundary below with actual recorded
operations and collection v2. The descriptions here retain the R7 landing's
scope; unrecorded legacy chronology still cannot be back-projected.

## R7b — recorded lengths at macro zoom

The ordinary Three view switches at 400% from overview enlargement to recorded
crystal lengths. Replay always uses recorded lengths. The visible zoom label
states which presentation is active. Parent bodies, inclusions and the contact
pre-pass share the same floor policy; zero-floor views do not invent large
newborn crystals. The cache changes once at a regime crossing, not on each
camera motion. Root positions and representative identities survive round trips.

This is length fidelity, not a claim of volume metrology: R4 mineral-form widths,
edge chamfers, modeled attachment fractions and inclusion placement remain
display approximations. The 0.3 mm quartz control measures 0.2994 mm after the
existing chamfer. Nominal length is before contact clipping; attachment or
modeled contact cuts can shorten the exposed body. Photo-rig close views use the same macro regime before subject
selection; cavity views use the normal UI zoom policy.

Evidence: `.local-evidence/photos/r7b-micro-single` (ordinary UI receipt plus two
angles), `.local-evidence/photos/r7b-topaz-actual` (accepted Shigar #13 at step70).
Focused scale, population and contact wiring checks: 22 tests across 3 files.
The first unprivileged run hit a Windows RSS-watchdog access error; the required
monitored rerun passed. No scientific records were modified.

Hostile review: **4/5 PASS** for R7b. Micro-scale transmission/highlight artifacts
remain an optics limitation, not a material-realism acceptance.

## R7c — contact presentation and cursor-safe weights

Both participants in a modeled contact now use net integrated length available
at the replay cursor. The old comment incorrectly called the signed
`total_growth_um` accumulator gross growth. Live weighting is preserved; missing
historical layers use the existing size-only fallback. This does not reconstruct
per-face competitive growth or date a scar.

Only actual group-1 contact caps receive a narrow dark boundary cue. Interior
triangulation edges cancel, including Float32 oblique caps. The cap remains
rough and opaque, preserves metallic substrate response, and is visible from
either side of a clear body. The helix restore path no longer copies a clear
host's transparency onto the cap; child rims also participate in sweep blending.
Owned clipped geometry and rims are disposed during rebuilds.

Evidence: `.local-evidence/photos/r7c-verified-on` and `r7c-verified-off`, fixed
camera, plus a profile aimed at the contact face. This controlled two-crystal
fixture modifies display inputs and is not a simulated geological outcome.
Earlier `r7c-contact-on`/`r7c-cap-face` photos exposed the now-fixed spoke and
helix-opacity defects. Four focused contact-history tests plus 20 existing
clipper/wiring tests pass; the new tests cover later growth, dissolution,
uncut controls, oblique seams, and sweep restoration.

Hostile review: **4/5 PASS** for R7c.

## R7d — surviving chemistry and current surface films

Clouds and colour use one ordered surviving-layer stack. Negative layers retreat
through newest material first, including partial retreat. The readers do not use
today's mutable remaining-layer counters during an earlier replay. Missing trace
data makes the bulk chemical colour unavailable instead of extrapolating a thin
known layer across unknown material. Present-day radiation damage is withheld
from earlier cursors because a dose chronology is not recorded.

The universal Fe/Mn/Ti trace-darkening heuristic is removed. Sphalerite Fe now
converts ppm to wt% explicitly (10,000 ppm = 1 wt%). A qualitative palette follows
the observed direction of Fe darkening in [Shen et al. (2020)](https://pubmed.ncbi.nlm.nih.gov/31892388/).
Its display endpoints span that study's 0.235–14.826 wt% range; this is neither a
calibrated spectrum nor a threshold defining marmatite. The fleet's tens of ppm
must not be presented as percent-level Fe. Other unsupported prose/unit rules
stay on mineral defaults. The inherited quartz damage palette remains a
qualitative current-state convention; smoky colour requires Al as well as damage,
consistent with [Nassau's experiments](https://www.gia.edu/gems-gemology/spring-1981-artificial-color-nassau).
No new colour-center occupancy model or per-zone irradiation history is claimed.

Supported sphalerite chemistry varies absorption along the existing bounded
convex-volume ray path. Sixteen bins map surviving axial growth into similar
volumes. There are no opaque composition interfaces. One homogeneous layer and
sixteen equivalent layers produce identical bins and pixel-identical hero,
isolated and profile images. The RGB coefficients and similar-volume geometry
are declared display approximations; unknown intervals use the default appearance.

Current exposed supported particulate-film coverage reads the live film record, including its surviving
contributors. Nominal contribution colours mix separately for prism and terminal
face classes; overlap locations are unrecorded. Coverage changes diffuse colour,
roughness, transmission and fallback alpha. No film is back-projected during
replay: breakthrough/later liberation can remove current operations, so the
present film cannot reconstruct the earlier exposed surface. Buried recorded
masked horizons retain R7a's separate surviving-band treatment. Clear-mineral or
unsupported contributors (including barite and quartz) withhold this new matte
coating treatment rather than becoming generic buff dust. Mixed unsupported
coverage is also withheld because separate exposed patch areas are not recorded.

Evidence: `.local-evidence/photos/r7d-iron-{zoned,uniform,split}` and
`r7d-film-{clear,coat,partial,alpha-clear,alpha-coat}`. All are explicitly labeled
modified-record fixtures. Forty-two checks across colour-history, body palette,
local colour and cloudy growth pass. The six history checks include equal-size
partial-record cache invalidation using the actual absorption bins.

An unmodified MVT seed-42 specimen (#45, 33.3182 ppm Fe) stays pale/resinous in
`.local-evidence/photos/r7d-fleet-probe/hero-2-sphalerite.png` and its isolated
image. Its profile is nearly invisible and is not accepted evidence. The first
candidate (#2) was an opaque enclosed guest, not an exposed material control.
Default hero selection now excludes such guests; explicit IDs retain diagnostics.

Hostile review: **4/5 PASS** for this bounded chemistry/film bridge.


## R7e — collected specimen history

New collections use `crystal-history-v1` without changing SAVE_FORMAT or the
collection epoch. Accepted zones are copied deeply, including Au/Sr/Co,
per-zone aspect, formula/trace stoichiometry, solid-solution component/activity
maps, booked and remaining inventory, fluid inclusions and masked horizons.
Exact dimensions, volume and nucleation temperature are retained alongside
recorded tilt, current form descriptors, stress/twin testimony, etch/phase/
dehydration histories and source-scoped enclosure/CDR provenance. Enclosure
chains come from the actual owning simulator by object identity, including Zen.

Legacy records and pending collection/finish receipts authenticate through the
frozen legacy producer selected from the stored schema. Existing bytes and
digests are not upgraded or stripped. New scientific fields remain covered by
the complete science digest. An earlier collected snapshot is bound to its
authenticated receipt digest, preventing rehashed invented history or replacement
by the same crystal's later-grown state. Unknown schemas, malformed nested data
and unbounded history are rejected before import/reconstruction. Numeric legacy
zone counts remain readable without fabricated layers; missing traces and unknown
transition dates stay missing or unknown.

Collection reconstruction restores recorded self descriptors and exact size but
leaves old wall anchors, host IDs and CDR links inert outside their source cavity.
Current split/Wulff/film descriptors are final snapshots, not event chronologies.
An arbitrary imported standalone Library row is validated data, not independently
proven geological testimony; Creative receipts additionally authenticate by replay.

Validation: eight focused history cases (including real surface-growth, stress
and CDR producers), 11 player-name tests, and the complete 66-test fortress save
suite. Hostile review: **4/5 PASS** for R7e.

## R7f — recorded enclosure and dimensional replay

A single immutable projection reads the enclosure ledger only through the replay
cursor. The same projected relationships feed contact eligibility, inclusion
materials, cluster suppression, host lookup and guest placement. Liberation and
later re-enclosure are distinct events. Missing or invalid relationship history
is unavailable rather than a reason to borrow today's `enclosed_by` link.

Historical width mirrors the producer's per-zone ellipsoid-volume integration,
including changing recorded aspects and proportional volume loss on dissolution.
It no longer reinterprets old matter using the latest habit. Legacy aspect gaps
use an explicit neutral display ratio; the final split index is not used to
compact earlier dimensions because its chronology is unrecorded. Historical
physical presence also prevents a future hollow cast from replacing an earlier
solid crystal. Cast fallback requires a recorded complete dissolution and uses
the last pre-loss dimensions; no-growth cursors produce no cast or contact body.

These are consumers of existing records. The current mineral form template is
still used where per-face/habit chronology is absent; existing dated etch,
deformation and sceptre gates remain. The original inclusion placement inside a
host's rendered bounds is a display model, not a measured entrapment coordinate.
No full crystal-shape reconstruction, current-film timeline or radiation dose
history is claimed.

Nested enclosure placement resolves ancestors before descendants, including
ancestor display scaling. Cyclic replay relations are withheld. A verified
convex host additionally fits all transformed guest vertices inside its actual
half-spaces, using a deterministic interior display position and uniform display
reduction. Neither booked guest dimensions nor the recorded link changes.
Nonconvex hosts retain the prior bounding-sphere display and explicitly report
containment as unavailable; no measured entrapment position is claimed.

Validation: seven R7f tests plus 14 executed surface-fabric tests pass. The latter
routes a recorded prefix through the new projection and proves the earlier relief
stays at its small historical maturity. Six existing enclosure and four contact
wiring tests also pass, as do the 16 R7a population tests.

Final fixed-camera, two-angle sequence:
`.local-evidence/photos/r7f-contained-{2,3,5,8}`. These are modified-record
fixtures, not a simulated natural outcome. Steps 2/5 show the free patch; 3/8
show it contained in quartz. The 3/8 hero and profile are byte-identical and all
four manifests have zero runtime exceptions. Earlier `r7f-enclosure-*` captures
exposed protrusion through the narrow host and are superseded. Hostile review:
**4/5 PASS** for R7f after nested placement, cast and containment corrections.

## Completion boundary

R7a through R7f have each earned at least 4/5 in hostile review. This closes the
handoff's bounded visual bridge and preserves the available formation testimony.
Complete formation memory remains HOLD wherever the simulator does not record
per-face fronts, changing split/habit state, film liberation/breakthrough coverage,
colour-center occupancy or irradiation dose history. Age alone still supplies no
stain and an absent fluid-inclusion event still supplies no cloudy core.

Graphics quality controls and progressive refinement remain the requested final
polish note. Final frozen-runtime release/evidence validation is recorded below.

## Frozen-runtime validation

Typecheck and 238 tests across 20 serial renderer/regression files passed. Separate
focused runs passed the eight collection-history cases, 66 save tests, seven final
replay cases and 14 executed surface-fabric integration cases. Tests ran one worker
at a time under the required RSS watchdog. `git diff --check` is clean.

The required seed-42 Elmwood and tn457_barite_pulses cavity/hero/druse captures
completed with zero runtime exceptions. Counts are unchanged from R7a: Elmwood
16 simulation crystals / 123 display meshes; tn457 7 / 54. Measured rebuild/cached
redraw times were 431/9.8 ms and 256/5.7 ms on this workstation. These are small
fixture timings, not a promise about Bisbee or slower computers.

| Capture | R7a highlight → final | R7a edge → final |
| --- | --- | --- |
| Elmwood cavity | 0 → 0 | .01550 → .01056 |
| Elmwood fluorite hero | .00097 → .00093 | .02643 → .02265 |
| Elmwood druse | 0 → 0 | .02902 → .02958 |
| tn457 cavity | 0 → 0 | .00957 → .00797 |
| tn457 sphalerite hero | 0 → 0 | .03103 → .03287 |
| tn457 druse | 0 → 0 | .02887 → .03084 |

These handoff captures include the new macro framing policy and are not
same-camera material ablations. Controlled section-specific pairs above serve
that purpose. Contact sheets and full census/statistics are in
`.local-evidence/photos/r7-final-elmwood/contact-sheet.html` and
`.local-evidence/photos/r7-final-tn457_barite_pulses/contact-sheet.html`.

The official browser workflow passed all 17 player-facing checks and generated
its receipt normally. Its first run exposed a changed generated collection ID:
`cry-16-w8g` became `cry-16-qte` in the Simulation and Library journey records.
A recursive comparison found no other journey differences: geology fingerprints,
save IDs, strip digests and security assertions are unchanged. The exact constant
and matching test fixture were re-pinned without weakening validation; the hostile
reviewer independently confirmed the two-path-only difference. The subsequent
owned-browser rerun, receipt audit and all nine receipt tests passed.

The fresh commissioned Node 24.15.0 science rebake passed, including all 55
science tests across seven files. The aggregate authenticates 128 artifacts;
the locality audit reports zero unclassified products and zero contract
violations. Release generation/check, provenance and generated-build checks pass.

The three-seed frequency baseline, seed-42 calibration baseline, all 41 strip
stories (105.2 MB), and strip digest retain their committed contents under the
repository's LF-normalized hash policy. All 41 JSON and Markdown claim cards
change only the mechanism-witness payload hash link. Mechanism scientific
outcomes are unchanged; its guided-browser authority link changes with the new
browser receipt. Remaining evidence differences are runtime/producer fingerprints
and their derived authentication links. Receipts were generated by their official
producers, never edited by hand.

A fresh isolated checkout passed `npm ci` for both runtimes, typecheck, build and
generated-build checks, the complete checked-in CI audit sequence, the calibrated
supergene sentinel, 150 R7 regression tests across 13 serial files, and the agent
CLI help smoke. The checkout remained clean. This is the local cold-CI workflow,
not the all-files test suite or a hosted GitHub Actions run. The only subsequent
amendment records these validation results; executable, test, producer and
evidence contents remain identical to that tested checkout. Final-tree build,
science, evidence and release identity are checked again before push.

Execution logs: `.local-evidence/r7-final-evidence-workflow.log`,
`.local-evidence/r7-final-artifact-diff.log`, `.local-evidence/r7-final-cold-ci.log`,
and the corresponding `.local-evidence/r7-cold-*.log` files. GitHub's current
workflow runs on main pushes or pull requests; this feature-branch delivery
does not by itself commission a hosted CI run or deployment.
