{
  "name": "vugg-simulator",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling for the Vugg Simulator (TypeScript modules → inlined into index.html).",
  "type": "module",
  "scripts": {
    "build": "node tools/build-all.mjs",
    "build:check": "node tools/build-all.mjs --check",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "ci": "npm run typecheck && npm run build:check && npm test"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.5"
  }
}
